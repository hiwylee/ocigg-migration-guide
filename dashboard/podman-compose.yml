version: "3.8"

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./db:/app/db
      - ../scripts:/app/scripts:ro
      - ../plan:/app/plan:ro
      - ../docs:/app/docs:ro
    restart: unless-stopped
    networks:
      - dashboard

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - dashboard

networks:
  dashboard:
    driver: bridge

# 검증 단계 절차서 (Validation Runbook)

> **프로젝트**: AWS RDS (Oracle SE) → OCI DBCS (Oracle SE) 온라인 마이그레이션
> **문서 유형**: 운영 절차서 (Runbook / SOP)
> **적용 범위**: Phase 6 (검증) + Go/No-Go 판정 + Phase 7 (Cut-over) + Phase 8 (안정화)
> **참조 문서**: `plan/migration_plan.md`, `plan/02_migration.md`
> **검증 기준**: `docs/OCI_GG_Validation_Plan_20260313.xlsx` (6개 시트, 136개 항목)
> **문서 버전**: v1.0 | 작성일: 2026-03-17

---

## 문서 이력

| 버전 | 날짜 | 변경 내용 | 작성자 |
|------|------|-----------|--------|
| v1.0 | 2026-03-17 | 최초 작성 | |

---

## 담당자 및 역할

| 역할 | 담당 | 연락처 | 주요 책임 |
|------|------|--------|-----------|
| 마이그레이션 리더 | | | Go/No-Go 최종 판정 및 서명 |
| 소스 DBA (AWS) | | | 소스 측 검증 쿼리 실행, 비교 기준값 제공 |
| 타겟 DBA (OCI) | | | 타겟 측 검증 쿼리 실행, 결과 기록 |
| OCI GG 담당자 | | | GG 프로세스 검증, Cut-over 시 GG 조작 |
| 애플리케이션 담당 | | | Smoke Test, 기능 검증 |
| 비즈니스 담당 | | | 데이터 정합성 업무 확인, Go 최종 서명 |

---

## 전제 조건 (본 절차서 실행 전 확인)

> 아래 항목이 모두 완료되어야 본 절차서를 실행할 수 있습니다.

| # | 항목 | 확인 | 확인자 |
|---|------|------|--------|
| 1 | Phase 5 완료 체크리스트 전 항목 ✅ | ☐ | |
| 2 | GG Extract LAG < 30초 — 24시간 이상 연속 유지 | ☐ | |
| 3 | GG Replicat LAG < 30초 — 24시간 이상 연속 유지 | ☐ | |
| 4 | Discard 파일 레코드 0건 (누적) | ☐ | |
| 5 | HANDLECOLLISIONS 파라미터 제거 완료 | ☐ | |
| 6 | 3개 GG 프로세스 모두 RUNNING 상태 | ☐ | |

---

## 전체 검증 흐름

```
[Phase 6] 검증 (D-1 ~ D-Day)
  │
  ├─ STEP 01. GG 프로세스 검증           (01_GG_Process — 28항목)
  ├─ STEP 02. 정적 구조 검증             (02_Static_Schema — 38항목)
  │    ├─ 2-1. 테이블/컬럼 구조 비교
  │    ├─ 2-2. 인덱스 완전성 검증
  │    ├─ 2-3. 제약조건 검증
  │    ├─ 2-4. 시퀀스/Synonym 검증
  │    ├─ 2-5. 사용자/권한 비교
  │    └─ 2-6. INVALID 객체 0건 확인
  ├─ STEP 03. 데이터 정합성 검증         (03_Data_Validation — 25항목)
  │    ├─ 3-1. Row Count 비교
  │    ├─ 3-2. Checksum 비교
  │    ├─ 3-3. LOB 검증
  │    ├─ 3-4. 데이터 타입 특이사항
  │    └─ 3-5. 참조 무결성 검증
  ├─ STEP 04. 특수 객체 검증             (04_Special_Objects — 42항목)
  │    ├─ 4-1. MV 검증
  │    ├─ 4-2. Trigger 검증
  │    ├─ 4-3. DB Link 검증
  │    ├─ 4-4. DBMS_JOB/SCHEDULER 검증
  │    ├─ 4-5. Sequence 검증
  │    └─ 4-6. 파티션 테이블 검증
  ├─ STEP 05. 통계정보 검증              (통계 완전성)
  ├─ STEP 06. 주의사항 체크              (05_Migration_Caution — 26항목)
  ├─ STEP 07. Cut-over 직전 최종 확인    (오브젝트 완전성 + 인덱스 + INVALID)
  └─ STEP 08. Go/No-Go 판정             (최종 의사결정)
            ↓
         [Go 판정 시 → Cut-over 진행]
            ↓
[Phase 7] Cut-over
  ├─ STEP 09. Cut-over 사전 체크리스트
  ├─ STEP 10. Cut-over 실행
  └─ STEP 11. Cut-over 후 즉시 검증
            ↓
[Phase 8] 안정화
  └─ STEP 12. Cut-over 후 안정화 모니터링
```

---

## 표기 규칙

| 기호 | 의미 |
|------|------|
| `[소스]` | AWS RDS에서 실행 |
| `[타겟]` | OCI DBCS에서 실행 |
| `[GG]` | OCI GG Admin Server (GGSCI)에서 실행 |
| `[양쪽]` | 소스/타겟 양쪽에서 각각 실행 후 결과 비교 |
| ✅ PASS | 기대 결과와 일치 |
| ❌ FAIL | 불일치 — 즉시 중단 후 원인 분석 (No-Go 판정 사유) |
| ⚠️ WARN | 허용 범위 내 — 원인 기록 필수, 비즈니스 영향 분석 후 진행 |

---

# Phase 6: 검증 (Validation)

---

## STEP 01. GG 프로세스 검증 (01_GG_Process — 28항목)

**목적**: GG 복제 프로세스의 정상 동작 및 설정 적합성 전수 검증
**담당**: OCI GG 담당자, 소스/타겟 DBA
**예상 소요**: 1시간
**실행 환경**: `[GG]`, `[소스]`, `[타겟]`

---

### 1-1. Pre-Check 항목 (#1~9)

#### #1 ENABLE_GOLDENGATE_REPLICATION

```sql
-- [소스] 실행
SELECT NAME, VALUE FROM V$PARAMETER
WHERE NAME = 'enable_goldengate_replication';
-- 기대값: TRUE
```

| 항목 | 소스 값 | 기대값 | 판정 |
|------|--------|--------|------|
| enable_goldengate_replication | | TRUE | ☐ PASS ☐ FAIL |

#### #2 Archive Log 모드

```sql
-- [소스] 실행
SELECT LOG_MODE FROM V$DATABASE;
-- 기대값: ARCHIVELOG
```

#### #3 Supplemental Logging (MIN) 활성화

```sql
-- [소스] 실행
SELECT SUPPLEMENTAL_LOG_DATA_MIN FROM V$DATABASE;
-- 기대값: YES
```

#### #4 PK 없는 테이블 ALL COLUMNS 로깅

```sql
-- [소스] PK 없는 테이블 수 vs ALL COLUMNS 로깅 적용 수 비교
SELECT
    (SELECT COUNT(*)
     FROM DBA_TABLES T
     WHERE T.OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS','AUDSYS','CTXSYS',
         'DBSFWUSER','DVSYS','GGSYS','GSMADMIN_INTERNAL','LBACSYS','MDSYS','OJVMSYS',
         'OLAPSYS','ORDDATA','ORDSYS','WMSYS','XDB','XS$NULL','GGADMIN','GGS_TEMP','MIGRATION_USER')
     AND T.TEMPORARY = 'N' AND T.EXTERNAL = 'NO'
     AND NOT EXISTS (
         SELECT 1 FROM DBA_CONSTRAINTS C
         WHERE C.OWNER = T.OWNER AND C.TABLE_NAME = T.TABLE_NAME AND C.CONSTRAINT_TYPE = 'P'
     )
    ) AS NO_PK_TABLES,
    (SELECT COUNT(DISTINCT LOG_GROUP_TABLE)
     FROM DBA_LOG_GROUPS WHERE LOG_GROUP_TYPE = 'USER LOG GROUP'
    ) AS SUPLOG_APPLIED
FROM DUAL;
-- 기대값: NO_PK_TABLES = SUPLOG_APPLIED
```

#### #5 Redo Log 그룹/크기

```sql
-- [소스] 실행
SELECT GROUP#, MEMBERS, BYTES/1024/1024 AS MB, STATUS FROM V$LOG;
-- 기대값: 최소 3그룹, 그룹당 500MB 이상
```

#### #6 GGADMIN 계정 권한

```sql
-- [소스] / [타겟] 각각 실행
SELECT GRANTEE, PRIVILEGE FROM DBA_SYS_PRIVS WHERE GRANTEE = 'GGADMIN'
UNION ALL
SELECT GRANTEE, GRANTED_ROLE FROM DBA_ROLE_PRIVS WHERE GRANTEE = 'GGADMIN';
```

#### #7 RDS Redo Log 보존 기간

```sql
-- [소스] 실행
SELECT NAME, VALUE FROM V$PARAMETER WHERE NAME LIKE 'archivelog%';
-- 기대값: 48시간 이상
```

#### #8 GG Deployment 버전 확인

```
[OCI콘솔] GoldenGate → Deployments → 버전 확인
-- Oracle 19c SE 지원 버전 확인
```

#### #9 네트워크 연결 (소스 → OCI GG 구간)

```bash
# [로컬] OCI GG에서 소스 RDS 접근 가능 여부
sqlplus GGADMIN/password@<RDS_ENDPOINT>:1521/<SERVICE>
-- 기대값: 접속 성공
```

### 1-2. Extract 상태 항목 (#10~14)

```
-- [GG] GGSCI에서 실행
STATUS EXTRACT EXT1
-- 기대값: RUNNING

LAG EXTRACT EXT1
-- 기대값: < 30초

INFO EXTRACT EXT1, DETAIL
-- Checkpoint SCN 지속 증가 확인

VIEW REPORT EXT1
-- ABEND/ERROR 없음 확인
```

### 1-3. Pump 상태 항목 (#15~17)

```
-- [GG] GGSCI에서 실행
STATUS EXTRACT PUMP1
-- 기대값: RUNNING

LAG EXTRACT PUMP1
-- 기대값: < 30초

INFO EXTRACT PUMP1, DETAIL
-- OCI Object Storage Trail 정상 전송 확인
```

### 1-4. Replicat 상태 항목 (#18~23)

```
-- [GG] GGSCI에서 실행
STATUS REPLICAT REP1
-- 기대값: RUNNING

LAG REPLICAT REP1
-- 기대값: < 30초

STATS REPLICAT REP1 TOTAL
-- Insert/Update/Delete 건수 증가 확인

VIEW REPORT REP1
-- ABEND/ERROR 없음 확인

VIEW DISCARD REP1
-- 기대값: 레코드 0건

-- SUPPRESSTRIGGERS 적용 확인 (파라미터 파일 확인)
VIEW PARAMS REP1
-- SUPPRESSTRIGGERS 라인 존재 확인
```

### 1-5. Trail File 항목 (#24~25)

```
-- [GG] GGSCI에서 실행
INFO EXTRACT EXT1, SHOWCH
-- Trail 용량 모니터링 (급증 없음 확인)
-- LOB 복제 시 Trail 급증 주의
```

### 1-6. 상시운영 항목 (#26~28)

```
[OCI콘솔] GoldenGate → Monitoring → Alarms
-- LAG > 30초 알람 설정 확인
-- ABEND 즉시 알람 설정 확인
-- 자동 업데이트(Auto Upgrade) 비활성화 확인
```

```sql
-- [타겟] GGADMIN 비밀번호 만료 정책
SELECT U.USERNAME, P.LIMIT AS PASSWORD_LIFE_TIME
FROM DBA_USERS U
JOIN DBA_PROFILES P ON P.PROFILE = U.PROFILE AND P.RESOURCE_NAME = 'PASSWORD_LIFE_TIME'
WHERE U.USERNAME = 'GGADMIN';
-- 기대값: UNLIMITED 또는 만료 전 갱신 절차 수립
```

### 1-7. STEP 01 검증 결과 요약

| 분류 | 항목 수 | PASS | FAIL | WARN | 비고 |
|------|--------|------|------|------|------|
| Pre-Check (#1~9) | 9 | | | | |
| Extract (#10~14) | 5 | | | | |
| Data Pump (#15~17) | 3 | | | | |
| Replicat (#18~23) | 6 | | | | |
| Trail File (#24~25) | 2 | | | | |
| 상시운영 (#26~28) | 3 | | | | |
| **합계** | **28** | | | | |

> **STEP 01 완료 서명**: __________________ 일시: __________________

---

## STEP 02. 정적 구조 검증 (02_Static_Schema — 38항목)

**목적**: 타겟 DB의 스키마 구조(테이블, 인덱스, 제약조건, 권한 등)가 소스와 완전 일치하는지 검증
**담당**: 소스/타겟 DBA
**예상 소요**: 2~3시간
**실행 환경**: `[양쪽]` (소스/타겟 동일 쿼리 실행 후 MINUS 비교)

---

### 2-1. 테이블/컬럼 구조 비교 (#1~5)

```sql
-- [양쪽] 동일 쿼리 실행 — 결과 MINUS 비교 (소스 기준 타겟에 없는 것 확인)
SELECT OWNER, TABLE_NAME, COLUMN_NAME, COLUMN_ID,
       DATA_TYPE, DATA_LENGTH, DATA_PRECISION, DATA_SCALE, NULLABLE, DATA_DEFAULT
FROM DBA_TAB_COLUMNS
WHERE OWNER NOT IN (
    'SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS','AUDSYS','CTXSYS',
    'DBSFWUSER','DVSYS','GGSYS','GSMADMIN_INTERNAL','LBACSYS',
    'MDSYS','OJVMSYS','OLAPSYS','ORDDATA','ORDSYS','WMSYS','XDB','XS$NULL'
)
ORDER BY OWNER, TABLE_NAME, COLUMN_ID;
-- 소스 결과 MINUS 타겟 결과 → 기대값: 0건
```

```sql
-- [양쪽] 테이블 압축 속성 비교
SELECT OWNER, TABLE_NAME, COMPRESSION, COMPRESS_FOR
FROM DBA_TABLES
WHERE OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
AND COMPRESSION = 'ENABLED'
ORDER BY OWNER, TABLE_NAME;
-- 소스에 압축 테이블 존재 시 타겟도 동일 방식 적용 여부 확인
```

| 항목 | 소스 COUNT | 타겟 COUNT | MINUS 건수 | 판정 |
|------|-----------|-----------|-----------|------|
| 전체 테이블 수 | | | | ☐ PASS ☐ FAIL |
| 전체 컬럼 수 | | | | ☐ PASS ☐ FAIL |
| 압축 테이블 | | | | ☐ PASS ☐ WARN |

### 2-2. 인덱스 완전성 검증 (#6~9)

```sql
-- [양쪽] ① 인덱스 전체 목록 비교 (Invisible 포함)
SELECT OWNER, INDEX_NAME, TABLE_NAME, INDEX_TYPE, UNIQUENESS, STATUS, VISIBILITY, PARTITIONED
FROM DBA_INDEXES
WHERE OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
ORDER BY OWNER, TABLE_NAME, INDEX_NAME;
-- 소스 MINUS 타겟 → 기대값: 0건

-- [양쪽] ② 인덱스 컬럼 구성 비교 (컬럼 순서 포함)
SELECT INDEX_OWNER, INDEX_NAME, TABLE_NAME, COLUMN_POSITION, COLUMN_NAME, DESCEND
FROM DBA_IND_COLUMNS
WHERE INDEX_OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
ORDER BY INDEX_OWNER, INDEX_NAME, COLUMN_POSITION;

-- [양쪽] ③ Function-Based Index (FBI) 표현식 비교
SELECT INDEX_OWNER, INDEX_NAME, TABLE_NAME, COLUMN_POSITION, COLUMN_EXPRESSION, DESCEND
FROM DBA_IND_EXPRESSIONS
WHERE INDEX_OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
ORDER BY INDEX_OWNER, INDEX_NAME, COLUMN_POSITION;
```

```sql
-- [타겟] ④ 전체 인덱스 VALID 상태 (UNUSABLE 0건)
SELECT OWNER, INDEX_NAME, TABLE_NAME, STATUS, PARTITIONED
FROM DBA_INDEXES
WHERE STATUS NOT IN ('VALID','N/A')
AND OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS');
-- 기대값: 0건

-- [타겟] ⑤ 파티션 인덱스 파티션별 USABLE 확인
SELECT INDEX_OWNER, INDEX_NAME, PARTITION_NAME, STATUS
FROM DBA_IND_PARTITIONS
WHERE STATUS != 'USABLE'
AND INDEX_OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS');
-- 기대값: 0건

-- [타겟] ⑥ 인덱스 통계 존재 여부 (LAST_ANALYZED IS NULL 0건)
SELECT OWNER, INDEX_NAME, TABLE_NAME, LAST_ANALYZED
FROM DBA_IND_STATISTICS
WHERE OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
AND LAST_ANALYZED IS NULL
ORDER BY OWNER, INDEX_NAME;
-- 기대값: 0건
```

| 항목 | 소스 COUNT | 타겟 COUNT | MINUS 건수 | 판정 |
|------|-----------|-----------|-----------|------|
| 전체 인덱스 수 | | | | ☐ PASS ☐ FAIL |
| FBI 인덱스 수 | | | | ☐ PASS ☐ FAIL |
| Invisible 인덱스 수 | | | | ☐ PASS ☐ FAIL |
| UNUSABLE 인덱스 | | 0 | | ☐ PASS ☐ FAIL |
| 통계 없는 인덱스 | | 0 | | ☐ PASS ☐ FAIL |

### 2-3. 제약조건 검증 (#10~15)

```sql
-- [양쪽] 제약조건 전수 비교 (PK/UK/FK/CHECK)
SELECT OWNER, CONSTRAINT_NAME, TABLE_NAME, CONSTRAINT_TYPE, STATUS,
       R_OWNER, R_CONSTRAINT_NAME, DELETE_RULE
FROM DBA_CONSTRAINTS
WHERE OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
AND CONSTRAINT_TYPE IN ('P','U','R','C')
ORDER BY OWNER, TABLE_NAME, CONSTRAINT_TYPE;
-- 소스 MINUS 타겟 → 기대값: 0건

-- [타겟] DISABLED 제약조건 확인 (FK는 의도적 DISABLE — Cut-over 후 ENABLE 예정)
SELECT OWNER, TABLE_NAME, CONSTRAINT_NAME, CONSTRAINT_TYPE
FROM DBA_CONSTRAINTS
WHERE STATUS = 'DISABLED'
AND CONSTRAINT_TYPE != 'R'   -- FK는 제외 (복제 중 의도적 DISABLE)
AND OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS');
-- 기대값: 0건 (FK 외 PK/UK/CHECK는 DISABLED 없음)
```

| 항목 | 소스 COUNT | 타겟 COUNT | MINUS 건수 | 판정 |
|------|-----------|-----------|-----------|------|
| PK 제약 수 | | | | ☐ PASS ☐ FAIL |
| FK 제약 수 | | | | ☐ PASS ☐ FAIL |
| UNIQUE 제약 수 | | | | ☐ PASS ☐ FAIL |
| CHECK 제약 수 | | | | ☐ PASS ☐ FAIL |
| FK 외 DISABLED 제약 | | 0 | | ☐ PASS ☐ FAIL |

### 2-4. 시퀀스 및 Synonym 검증 (#19~23)

```sql
-- [양쪽] 시퀀스 전체 속성 비교
SELECT SEQUENCE_OWNER, SEQUENCE_NAME,
       MIN_VALUE, MAX_VALUE, INCREMENT_BY,
       CYCLE_FLAG, ORDER_FLAG, CACHE_SIZE, LAST_NUMBER
FROM DBA_SEQUENCES
WHERE SEQUENCE_OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
ORDER BY SEQUENCE_OWNER, SEQUENCE_NAME;
-- 타겟 LAST_NUMBER ≥ 소스 LAST_NUMBER + CACHE_SIZE 확인
-- INCREMENT_BY / CYCLE / ORDER / CACHE 소스와 완전 일치 확인
```

```sql
-- [양쪽] Synonym 전수 비교 (Public 포함)
SELECT OWNER, SYNONYM_NAME, OWNER, TABLE_NAME, DB_LINK
FROM DBA_SYNONYMS
WHERE (OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS') OR OWNER = 'PUBLIC')
ORDER BY OWNER, SYNONYM_NAME;
-- 소스 MINUS 타겟 → 기대값: 0건

-- [타겟] 참조 객체 없는 Synonym (Dangling Synonym)
SELECT S.OWNER, S.SYNONYM_NAME, S.OWNER, S.TABLE_NAME
FROM DBA_SYNONYMS S
WHERE NOT EXISTS (
    SELECT 1 FROM DBA_OBJECTS O
    WHERE O.OWNER = S.OWNER AND O.OBJECT_NAME = S.TABLE_NAME
)
AND S.DB_LINK IS NULL
AND S.OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS');
-- 기대값: 0건
```

| 항목 | 소스 COUNT | 타겟 COUNT | 판정 |
|------|-----------|-----------|------|
| 시퀀스 수 | | | ☐ PASS ☐ FAIL |
| 시퀀스 LAST_NUMBER (타겟 ≥ 소스+CACHE) | - | - | ☐ PASS ☐ WARN |
| Private Synonym 수 | | | ☐ PASS ☐ FAIL |
| Public Synonym 수 | | | ☐ PASS ☐ FAIL |
| Dangling Synonym | | 0 | ☐ PASS ☐ FAIL |

### 2-5. 사용자/권한 비교 (#29~32)

```sql
-- [양쪽] 사용자 목록 및 Profile
SELECT USERNAME, ACCOUNT_STATUS, DEFAULT_TABLESPACE, TEMPORARY_TABLESPACE, PROFILE
FROM DBA_USERS
WHERE USERNAME NOT IN (
    'SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS','AUDSYS','CTXSYS',
    'DBSFWUSER','DVSYS','GGSYS','GSMADMIN_INTERNAL','GSMCATUSER',
    'GSMUSER','LBACSYS','OJVMSYS','OLAPSYS','ORACLE_OCM','ORDDATA',
    'ORDPLUGINS','ORDSYS','SI_INFORMTN_SCHEMA','SYS$UMF','SYSBACKUP',
    'SYSDG','SYSKM','SYSRAC','WMSYS','XDB','XS$NULL'
)
ORDER BY USERNAME;

-- [양쪽] 롤 부여 현황 비교
SELECT GRANTEE, GRANTED_ROLE, ADMIN_OPTION FROM DBA_ROLE_PRIVS
WHERE GRANTEE NOT IN ('SYS','SYSTEM','DBA','IMP_FULL_DATABASE','EXP_FULL_DATABASE')
ORDER BY GRANTEE, GRANTED_ROLE;

-- [양쪽] 시스템 권한 비교
SELECT GRANTEE, PRIVILEGE, ADMIN_OPTION FROM DBA_SYS_PRIVS
WHERE GRANTEE NOT IN ('SYS','SYSTEM','DBA','IMP_FULL_DATABASE','EXP_FULL_DATABASE')
ORDER BY GRANTEE, PRIVILEGE;

-- [양쪽] 객체 권한 비교
SELECT GRANTEE, OWNER, TABLE_NAME, PRIVILEGE, GRANTABLE FROM DBA_TAB_PRIVS
WHERE GRANTEE NOT IN ('SYS','SYSTEM','DBA','PUBLIC','WMSYS')
ORDER BY GRANTEE, OWNER, TABLE_NAME;
```

| 항목 | 소스 COUNT | 타겟 COUNT | MINUS 건수 | 판정 |
|------|-----------|-----------|-----------|------|
| 사용자 수 | | | | ☐ PASS ☐ FAIL |
| 롤 부여 건수 | | | | ☐ PASS ☐ FAIL |
| 시스템 권한 건수 | | | | ☐ PASS ☐ FAIL |
| 객체 권한 건수 | | | | ☐ PASS ☐ WARN |

### 2-6. INVALID 객체 0건 확인 (#37~38)

```sql
-- [타겟] INVALID 객체 전수 확인
SELECT OWNER, OBJECT_TYPE, OBJECT_NAME, STATUS, LAST_DDL_TIME
FROM DBA_OBJECTS
WHERE STATUS = 'INVALID'
AND OWNER NOT IN (
    'SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS','AUDSYS','CTXSYS',
    'DBSFWUSER','DVSYS','GGSYS','GSMADMIN_INTERNAL','LBACSYS',
    'MDSYS','OJVMSYS','OLAPSYS','ORDDATA','ORDSYS','WMSYS','XDB'
)
ORDER BY OWNER, OBJECT_TYPE;
-- 기대값: 0건 → INVALID 있으면 즉시 재컴파일
```

```sql
-- [타겟] INVALID 존재 시 전체 DB 재컴파일
BEGIN
    FOR u IN (
        SELECT USERNAME FROM DBA_USERS
        WHERE USERNAME NOT IN (
            'SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS','AUDSYS','CTXSYS',
            'DBSFWUSER','DVSYS','EXFSYS','GGSYS','GSMADMIN_INTERNAL','GSMCATUSER',
            'GSMUSER','LBACSYS','MDSYS','OJVMSYS','OLAPSYS','ORACLE_OCM','ORDDATA',
            'ORDPLUGINS','ORDSYS','REMOTE_SCHEDULER_AGENT','SI_INFORMTN_SCHEMA',
            'SYS$UMF','SYSBACKUP','SYSDG','SYSKM','SYSRAC','WMSYS','XDB','XS$NULL',
            'GGADMIN','GGS_TEMP','MIGRATION_USER'
        )
        AND ACCOUNT_STATUS = 'OPEN'
        ORDER BY USERNAME
    ) LOOP
        DBMS_UTILITY.COMPILE_SCHEMA(schema => u.USERNAME, compile_all => FALSE);
    END LOOP;
END;
/

-- 재컴파일 후 재확인 — 기대값: 0건
SELECT COUNT(*) AS INVALID_CNT FROM DBA_OBJECTS
WHERE STATUS = 'INVALID'
AND OWNER NOT IN (
    'SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS','AUDSYS','CTXSYS',
    'DBSFWUSER','DVSYS','GGSYS','GSMADMIN_INTERNAL','LBACSYS',
    'MDSYS','OJVMSYS','OLAPSYS','ORDDATA','ORDSYS','WMSYS','XDB'
);
```

### 2-7. STEP 02 검증 결과 요약

| 분류 | 항목 수 | PASS | FAIL | WARN | 비고 |
|------|--------|------|------|------|------|
| 테이블/컬럼 구조 (#1~5) | 5 | | | | |
| 인덱스 (#6~9) | 4 | | | | |
| 제약조건 (#10~15) | 6 | | | | |
| Directory/Ext Table (#33~36) | 4 | | | | |
| INVALID 객체 (#37~38) | 2 | | | | |
| 시퀀스/Synonym (#19~23) | 5 | | | | |
| 사용자/권한 (#29~32) | 4 | | | | |
| PL/SQL 구조 (#24~28) | 5 | | | | |
| 기타 Tablespace (#33~34) | 3 | | | | |
| **합계** | **38** | | | | |

> **STEP 02 완료 서명**: __________________ 일시: __________________

---

## STEP 03. 데이터 정합성 검증 (03_Data_Validation — 25항목)

**목적**: 소스/타겟 데이터 내용이 GG 복제 기준으로 일치하는지 검증
**담당**: 소스/타겟 DBA, 비즈니스 담당
**예상 소요**: 3~4시간
**실행 환경**: `[양쪽]`

---

### 3-1. 전체 Row Count 비교 (#1~3)

```sql
-- [양쪽] 핵심 테이블 Row Count 비교 스크립트 생성
SELECT 'SELECT ''' || OWNER || '.' || TABLE_NAME || ''' AS TBL, COUNT(*) AS CNT FROM '
       || OWNER || '.' || TABLE_NAME || ';'
FROM DBA_TABLES
WHERE OWNER NOT IN (
    'SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS','AUDSYS','CTXSYS',
    'DBSFWUSER','DVSYS','GGSYS','GSMADMIN_INTERNAL','LBACSYS',
    'MDSYS','OJVMSYS','OLAPSYS','ORDDATA','ORDSYS','WMSYS','XDB','XS$NULL',
    'GGADMIN','GGS_TEMP','MIGRATION_USER'
)
ORDER BY OWNER, TABLE_NAME;
-- 생성된 스크립트 실행 후 소스/타겟 비교
```

```sql
-- [양쪽] 대용량 테이블 파티션 단위 비교 (#2)
SELECT OWNER, TABLE_NAME, PARTITION_NAME, NUM_ROWS
FROM DBA_TAB_PARTITIONS
WHERE OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP')
AND NUM_ROWS > 10000000
ORDER BY OWNER, TABLE_NAME, PARTITION_POSITION;
```

> **Row Count 비교 기준**: GG 복제 실시간 동기화 중이므로 쿼리 실행 시점 차이에 의한 ±수 건 오차는 허용
> 단, 1% 이상 차이 발생 시 WARN 처리 후 원인 분석 필수

| 스키마 | 테이블 | 소스 COUNT | 타겟 COUNT | 차이율 | 판정 |
|--------|--------|-----------|-----------|--------|------|
| | | | | | ☐ PASS ☐ WARN ☐ FAIL |
| | | | | | ☐ PASS ☐ WARN ☐ FAIL |
| | | | | | ☐ PASS ☐ WARN ☐ FAIL |

### 3-2. Checksum 비교 (#4~6)

```sql
-- [양쪽] ORA_HASH 기반 체크섬 (ROWID 절대 사용 금지 — 소스/타겟 ROWID 다름)
-- PK + 핵심 컬럼 기반 체크섬
SELECT
    COUNT(*) AS CNT,
    SUM(ORA_HASH(
        TO_CHAR(<PK_COL>) || '|' ||
        TO_CHAR(<COL1>)   || '|' ||
        TO_CHAR(<COL2>)
    )) AS CHECKSUM
FROM <SCHEMA>.<TABLE_NAME>;
-- 소스/타겟 CNT와 CHECKSUM 모두 일치해야 함

-- [양쪽] 핵심 테이블 집계 비교
SELECT
    SUM(<AMOUNT_COL>) AS TOTAL_SUM,
    MAX(<AMOUNT_COL>) AS MAX_VAL,
    MIN(<AMOUNT_COL>) AS MIN_VAL,
    COUNT(DISTINCT <KEY_COL>) AS DISTINCT_KEY_CNT
FROM <SCHEMA>.<TABLE_NAME>;
```

| 테이블 | 소스 CNT | 타겟 CNT | 소스 CHECKSUM | 타겟 CHECKSUM | 판정 |
|--------|---------|---------|--------------|--------------|------|
| | | | | | ☐ PASS ☐ FAIL |
| | | | | | ☐ PASS ☐ FAIL |

### 3-3. 샘플 데이터 비교 (#7~9)

```sql
-- [양쪽] 최신 1000건 비교 (PK 기준 정렬)
SELECT <PK_COL>, <COL1>, <COL2>, <UPDATED_DATE>
FROM <SCHEMA>.<TABLE_NAME>
ORDER BY <UPDATED_DATE> DESC
FETCH FIRST 1000 ROWS ONLY;

-- [양쪽] NULL 비율 비교 (핵심 컬럼 대상)
SELECT
    COUNT(*) AS TOTAL,
    SUM(CASE WHEN <COL> IS NULL THEN 1 ELSE 0 END) AS NULL_CNT,
    ROUND(SUM(CASE WHEN <COL> IS NULL THEN 1 ELSE 0 END)/COUNT(*)*100,2) AS NULL_PCT
FROM <SCHEMA>.<TABLE_NAME>;
```

### 3-4. LOB 검증 (#10~14)

```sql
-- [양쪽] LOB 건수 및 크기 비교
SELECT L.TABLE_NAME, L.COLUMN_NAME,
       COUNT(*) AS ROW_CNT,
       SUM(DBMS_LOB.GETLENGTH(T.<LOB_COL>)) AS TOTAL_BYTES
FROM <SCHEMA>.<TABLE_NAME> T
JOIN DBA_LOBS L ON L.OWNER = '<SCHEMA>'
    AND L.TABLE_NAME = '<TABLE_NAME>' AND L.COLUMN_NAME = '<LOB_COL>'
GROUP BY L.TABLE_NAME, L.COLUMN_NAME;

-- [양쪽] EMPTY_LOB vs NULL 처리 일관성
SELECT
    SUM(CASE WHEN <LOB_COL> IS NULL THEN 1 ELSE 0 END) AS NULL_CNT,
    SUM(CASE WHEN DBMS_LOB.GETLENGTH(<LOB_COL>) = 0 THEN 1 ELSE 0 END) AS EMPTY_CNT
FROM <SCHEMA>.<TABLE_NAME>;
```

```sql
-- [타겟] BasicFile → SecureFile 전환 여부 확인
SELECT OWNER, TABLE_NAME, COLUMN_NAME, SECUREFILE
FROM DBA_LOBS
WHERE OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP');
-- 소스와 동일한 유형(SECUREFILE YES/NO) 유지 여부 확인
```

### 3-5. 데이터 타입 특이사항 검증 (#15~20)

```sql
-- [양쪽] DATE 컬럼 시간 정보 유실 여부 확인
SELECT TO_CHAR(<DATE_COL>, 'YYYY-MM-DD HH24:MI:SS')
FROM <SCHEMA>.<TABLE_NAME>
WHERE <DATE_COL> > SYSDATE - 7
FETCH FIRST 100 ROWS ONLY;
-- 시:분:초가 00:00:00으로 유실되지 않았는지 확인

-- [양쪽] TIMESTAMP WITH TIME ZONE 확인
SELECT DBTIMEZONE FROM DUAL;
SELECT PARAMETER, VALUE FROM NLS_DATABASE_PARAMETERS
WHERE PARAMETER = 'NLS_TIMESTAMP_TZ_FORMAT';
-- 소스/타겟 동일 확인

-- [양쪽] XMLTYPE 스토리지 방식 확인
SELECT OWNER, TABLE_NAME, COLUMN_NAME, STORAGE
FROM DBA_XML_TABLES
WHERE OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP');
```

```sql
-- [타겟] ROWID 의존 소스 코드 식별 (애플리케이션 레벨 점검 필요)
SELECT OWNER, NAME, TYPE FROM DBA_DEPENDENCIES
WHERE REFERENCED_TYPE = 'TABLE'
AND NAME IN (
    SELECT DISTINCT OWNER FROM DBA_SOURCE
    WHERE UPPER(TEXT) LIKE '%ROWID%'
    AND OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP')
);
-- ROWID 의존 PL/SQL 오브젝트 식별 → 애플리케이션 팀 확인 요청
```

### 3-6. NLS 검증 (#21~23)

```sql
-- [양쪽] 한글/특수문자 샘플 데이터 확인
SELECT <KOREAN_COL>,
       LENGTHB(<KOREAN_COL>) AS BYTE_LEN,
       LENGTH(<KOREAN_COL>) AS CHAR_LEN
FROM <SCHEMA>.<TABLE_NAME>
WHERE LENGTHB(<KOREAN_COL>) != LENGTH(<KOREAN_COL>)
FETCH FIRST 20 ROWS ONLY;
-- 소스/타겟 결과 일치 확인 (한글 깨짐 없음)
```

### 3-7. 참조 무결성 검증 (#24~25)

```sql
-- [타겟] Orphan Row 확인 (FK 참조 부모 없는 자식 레코드)
-- FK DISABLED 상태로 Import 시 무결성 오류 데이터 존재 가능
SELECT COUNT(*) AS ORPHAN_CNT
FROM <CHILD_SCHEMA>.<CHILD_TABLE> C
WHERE NOT EXISTS (
    SELECT 1 FROM <PARENT_SCHEMA>.<PARENT_TABLE> P
    WHERE P.<PK_COL> = C.<FK_COL>
);
-- 기대값: 0건

-- [타겟] 현재 DISABLED FK 목록 확인 (Cut-over 후 ENABLE 예정 목록)
SELECT OWNER, TABLE_NAME, CONSTRAINT_NAME, STATUS
FROM DBA_CONSTRAINTS
WHERE CONSTRAINT_TYPE = 'R' AND STATUS = 'DISABLED'
AND OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
ORDER BY OWNER, TABLE_NAME;
```

### 3-8. STEP 03 검증 결과 요약

| 분류 | 항목 수 | PASS | FAIL | WARN | 비고 |
|------|--------|------|------|------|------|
| Row Count 비교 (#1~3) | 3 | | | | |
| Checksum 비교 (#4~6) | 3 | | | | |
| 샘플 데이터 (#7~9) | 3 | | | | |
| LOB 검증 (#10~14) | 5 | | | | |
| 데이터 타입 특이사항 (#15~20) | 6 | | | | |
| NLS 검증 (#21~23) | 3 | | | | |
| 참조 무결성 (#24~25) | 2 | | | | |
| **합계** | **25** | | | | |

> **STEP 03 완료 서명**: __________________ 일시: __________________

---

## STEP 04. 특수 객체 검증 (04_Special_Objects — 42항목)

**목적**: MV, Trigger, DB Link, DBMS_JOB/SCHEDULER, Sequence, 파티션, DDL Replication 검증
**담당**: 소스/타겟 DBA, OCI GG 담당자
**예상 소요**: 2시간
**실행 환경**: `[양쪽]`, `[타겟]`

---

### 4-1. MV (Materialized View) 검증 (#1~8)

```sql
-- [양쪽] MV 목록 및 속성 비교
SELECT OWNER, MVIEW_NAME, REFRESH_METHOD, REFRESH_MODE, STALENESS, BUILD_MODE, REWRITE_ENABLED
FROM DBA_MVIEWS
WHERE OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP')
ORDER BY OWNER, MVIEW_NAME;
-- 소스 MINUS 타겟 → 기대값: 0건

-- [타겟] MV STALENESS 확인 (FRESH 또는 NEEDS_COMPILE 이외의 값 주의)
SELECT OWNER, MVIEW_NAME, STALENESS FROM DBA_MVIEWS
WHERE OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP');

-- [타겟] MV Row Count 확인 (소스와 비교)
SELECT COUNT(*) FROM <SCHEMA>.<MV_NAME>;

-- [타겟] MV REFRESH 동작 테스트
BEGIN
    DBMS_MVIEW.REFRESH('<SCHEMA>.<MV_NAME>', 'C');
END;
/
-- REFRESH 후 오류 없음 확인, STALENESS = 'FRESH' 확인
```

| 항목 | 소스 COUNT | 타겟 COUNT | 판정 |
|------|-----------|-----------|------|
| MV 수 | | | ☐ PASS ☐ FAIL |
| MV REFRESH 성공 | | | ☐ PASS ☐ FAIL |

### 4-2. Trigger 검증 (#9~15)

```sql
-- [양쪽] Trigger 목록 및 유형 비교
SELECT OWNER, TRIGGER_NAME, TRIGGER_TYPE, TRIGGERING_EVENT,
       OWNER, TABLE_NAME, STATUS
FROM DBA_TRIGGERS
WHERE OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP')
ORDER BY OWNER, TABLE_NAME, TRIGGER_NAME;
-- 소스 MINUS 타겟 → 기대값: 0건

-- [타겟] 현재 Trigger DISABLED 상태 확인 (복제 중 — 기대값: 모두 DISABLED)
SELECT OWNER, TRIGGER_NAME, STATUS FROM DBA_TRIGGERS
WHERE OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP')
AND STATUS = 'ENABLED';
-- 기대값: 0건 (복제 중에는 모두 DISABLED — Cut-over 후 ENABLE 예정)
```

| 항목 | 소스 COUNT | 타겟 COUNT | 판정 |
|------|-----------|-----------|------|
| Trigger 수 | | | ☐ PASS ☐ FAIL |
| ENABLED Trigger (복제 중) | | 0 | ☐ PASS ☐ WARN |

### 4-3. DB Link 검증 (#16~22)

```sql
-- [양쪽] DB Link 목록 비교
SELECT OWNER, DB_LINK, USERNAME, HOST FROM DBA_DB_LINKS
ORDER BY OWNER, DB_LINK;

-- [타겟] DB Link 연결 테스트
SELECT * FROM DUAL@<DB_LINK_NAME>;
-- 기대값: DUMMY = 'X'

-- [타겟] DB Link 의존 객체 컴파일 상태
SELECT OWNER, OBJECT_TYPE, OBJECT_NAME, STATUS
FROM DBA_OBJECTS
WHERE STATUS = 'INVALID'
AND OBJECT_NAME IN (
    SELECT DISTINCT NAME FROM DBA_DEPENDENCIES
    WHERE REFERENCED_TYPE = 'DATABASE LINK'
)
AND OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP');
-- 기대값: 0건
```

| 항목 | 소스 COUNT | 타겟 COUNT | 판정 |
|------|-----------|-----------|------|
| DB Link 수 | | | ☐ PASS ☐ FAIL |
| DB Link 연결 테스트 | | | ☐ PASS ☐ FAIL |

### 4-4. DBMS_JOB / DBMS_SCHEDULER 검증 (#23~29)

```sql
-- [소스] 기존 DBMS_JOB 목록 (BROKEN 처리 여부 확인)
SELECT JOB, LOG_USER, WHAT, INTERVAL, BROKEN, NEXT_DATE FROM DBA_JOBS
ORDER BY JOB;
-- 기대값: 모두 BROKEN = 'Y' (마이그레이션 중 이중 실행 방지)

-- [타겟] DBMS_SCHEDULER JOB 확인 (소스 DBMS_JOB을 SCHEDULER로 전환된 것)
SELECT JOB_NAME, OWNER, STATE, LAST_RUN_DURATION, NEXT_RUN_DATE, REPEAT_INTERVAL
FROM DBA_SCHEDULER_JOBS
WHERE OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
ORDER BY OWNER, JOB_NAME;
-- 소스 JOB WHAT ↔ 타겟 JOB 매핑 확인

-- [타겟] 실행 주기(INTERVAL) 소스와 동일 여부 확인
SELECT JOB_NAME, REPEAT_INTERVAL FROM DBA_SCHEDULER_JOBS
WHERE OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP');
```

| 항목 | 소스 COUNT | 타겟 COUNT | 판정 |
|------|-----------|-----------|------|
| DBMS_JOB 건수 (BROKEN 처리) | | - | ☐ PASS ☐ FAIL |
| DBMS_SCHEDULER JOB 건수 | | | ☐ PASS ☐ FAIL |
| JOB 실행 주기 일치 | - | - | ☐ PASS ☐ WARN |

### 4-5. Sequence 검증 (#30~33)

```sql
-- [소스] Sequence 현재값 + 필요 최솟값 계산
SELECT SEQUENCE_NAME, LAST_NUMBER, CACHE_SIZE,
       LAST_NUMBER + CACHE_SIZE AS REQUIRED_MIN_TARGET
FROM DBA_SEQUENCES
WHERE SEQUENCE_OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
ORDER BY SEQUENCE_OWNER, SEQUENCE_NAME;

-- [타겟] Sequence LAST_NUMBER 확인
SELECT SEQUENCE_OWNER, SEQUENCE_NAME, LAST_NUMBER, CACHE_SIZE
FROM DBA_SEQUENCES
WHERE SEQUENCE_OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
ORDER BY SEQUENCE_OWNER, SEQUENCE_NAME;
-- 타겟 LAST_NUMBER > 소스 REQUIRED_MIN_TARGET 확인
```

| 스키마 | Sequence명 | 소스 LAST | 소스 필요 최솟값 | 타겟 LAST | 판정 |
|--------|-----------|----------|--------------|---------|------|
| | | | | | ☐ PASS ☐ FAIL |
| | | | | | ☐ PASS ☐ FAIL |

### 4-6. 파티션 테이블 검증 (#34~38)

```sql
-- [양쪽] 파티션별 Row Count 비교
SELECT OWNER, TABLE_NAME, PARTITION_NAME, NUM_ROWS, HIGH_VALUE
FROM DBA_TAB_PARTITIONS
WHERE OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP')
ORDER BY OWNER, TABLE_NAME, PARTITION_POSITION;

-- [타겟] 파티션 Pruning 동작 확인 (실행계획 확인)
EXPLAIN PLAN FOR
SELECT * FROM <SCHEMA>.<PARTITION_TABLE>
WHERE <PARTITION_KEY> = <VALUE>;
SELECT * FROM TABLE(DBMS_XPLAN.DISPLAY);
-- 기대값: "PARTITION RANGE SINGLE" 또는 특정 파티션만 접근
```

### 4-7. DDL Replication 검증 (#39~42)

```sql
-- DDL 복제 테스트 (테스트용 테이블로 진행)
-- [소스] 테스트 테이블 생성
CREATE TABLE MIGRATION_USER.GG_DDL_TEST (ID NUMBER, NAME VARCHAR2(100));

-- [타겟] GG 복제 후 테이블 생성 여부 확인 (30초 대기)
SELECT TABLE_NAME FROM DBA_TABLES WHERE OWNER = 'MIGRATION_USER' AND TABLE_NAME = 'GG_DDL_TEST';
-- 기대값: GG_DDL_TEST 조회

-- [소스] 컬럼 추가 DDL
ALTER TABLE MIGRATION_USER.GG_DDL_TEST ADD (CREATED_DATE DATE);

-- [타겟] 컬럼 추가 복제 확인
SELECT COLUMN_NAME FROM DBA_TAB_COLUMNS
WHERE OWNER = 'MIGRATION_USER' AND TABLE_NAME = 'GG_DDL_TEST'
ORDER BY COLUMN_ID;
-- 기대값: ID, NAME, CREATED_DATE 3개 컬럼

-- [소스] 테스트 테이블 정리
DROP TABLE MIGRATION_USER.GG_DDL_TEST PURGE;
```

| 항목 | 판정 |
|------|------|
| CREATE TABLE DDL 복제 | ☐ PASS ☐ FAIL ☐ WARN |
| ALTER TABLE DDL 복제 | ☐ PASS ☐ FAIL ☐ WARN |
| DROP TABLE DDL 복제 | ☐ PASS ☐ FAIL ☐ WARN |

> SE 환경에서 미지원 DDL 유형은 수동 처리 절차 문서화 필요

### 4-8. STEP 04 검증 결과 요약

| 분류 | 항목 수 | PASS | FAIL | WARN | 비고 |
|------|--------|------|------|------|------|
| MV (#1~8) | 8 | | | | |
| Trigger (#9~15) | 7 | | | | |
| DB Link (#16~22) | 7 | | | | |
| DBMS_JOB/SCHEDULER (#23~29) | 7 | | | | |
| Sequence (#30~33) | 4 | | | | |
| 파티션 테이블 (#34~38) | 5 | | | | |
| DDL Replication (#39~42) | 4 | | | | |
| **합계** | **42** | | | | |

> **STEP 04 완료 서명**: __________________ 일시: __________________

---

## STEP 05. 통계정보 검증

**목적**: 타겟 DB의 모든 테이블/인덱스/컬럼에 유효한 통계정보가 존재하는지 확인
**담당**: 타겟 DBA
**예상 소요**: 30분
**실행 환경**: `[타겟]`

---

### 5-1. 테이블 통계 완전성 확인

```sql
-- [타겟] 통계 없는 테이블 0건 확인
SELECT OWNER, TABLE_NAME, NUM_ROWS, BLOCKS, AVG_ROW_LEN, LAST_ANALYZED
FROM DBA_TABLES
WHERE OWNER NOT IN (
    'SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS','AUDSYS','CTXSYS',
    'DBSFWUSER','DVSYS','GGSYS','GSMADMIN_INTERNAL','LBACSYS',
    'MDSYS','OJVMSYS','OLAPSYS','ORDDATA','ORDSYS','WMSYS','XDB','XS$NULL'
)
AND LAST_ANALYZED IS NULL
ORDER BY OWNER, TABLE_NAME;
-- 기대값: 0건

-- [타겟] 통계가 7일 이상 경과한 테이블 (Cut-over 전 재수집 검토)
SELECT OWNER, TABLE_NAME, LAST_ANALYZED,
       ROUND(SYSDATE - LAST_ANALYZED) AS DAYS_OLD
FROM DBA_TABLES
WHERE OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
AND LAST_ANALYZED < SYSDATE - 7
ORDER BY DAYS_OLD DESC;
```

### 5-2. 인덱스 통계 완전성 확인

```sql
-- [타겟] 통계 없는 인덱스 0건 확인
SELECT OWNER, INDEX_NAME, TABLE_NAME, LAST_ANALYZED
FROM DBA_IND_STATISTICS
WHERE OWNER NOT IN (
    'SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS','AUDSYS','CTXSYS',
    'DBSFWUSER','DVSYS','GGSYS','GSMADMIN_INTERNAL','LBACSYS',
    'MDSYS','OJVMSYS','OLAPSYS','ORDDATA','ORDSYS','WMSYS','XDB'
)
AND LAST_ANALYZED IS NULL
ORDER BY OWNER, TABLE_NAME;
-- 기대값: 0건
```

### 5-3. 컬럼 통계 및 히스토그램 확인

```sql
-- [양쪽] 히스토그램 보유 컬럼 비교 (소스/타겟 동일 컬럼에 히스토그램 존재 여부)
SELECT OWNER, TABLE_NAME, COLUMN_NAME, HISTOGRAM, NUM_BUCKETS, LAST_ANALYZED
FROM DBA_TAB_COL_STATISTICS
WHERE OWNER NOT IN (
    'SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS','AUDSYS','CTXSYS',
    'DBSFWUSER','DVSYS','GGSYS','GSMADMIN_INTERNAL','LBACSYS',
    'MDSYS','OJVMSYS','OLAPSYS','ORDDATA','ORDSYS','WMSYS','XDB'
)
AND HISTOGRAM != 'NONE'
ORDER BY OWNER, TABLE_NAME, COLUMN_NAME;
-- 소스 MINUS 타겟 → 기대값: 0건 (동일 컬럼에 히스토그램 존재)
```

### 5-4. 통계 부족 시 즉시 수집

```sql
-- [타겟] 특정 테이블 통계 즉시 수집 (통계 없는 테이블 발견 시)
BEGIN
    DBMS_STATS.GATHER_TABLE_STATS(
        ownname          => '<SCHEMA>',
        tabname          => '<TABLE_NAME>',
        estimate_percent => DBMS_STATS.AUTO_SAMPLE_SIZE,
        cascade          => TRUE,
        method_opt       => 'FOR ALL COLUMNS SIZE AUTO',
        no_invalidate    => FALSE
    );
END;
/
```

### 5-5. 결과 기록

| 항목 | 확인 결과 | 판정 |
|------|-----------|------|
| 통계 없는 테이블 | 건 | ☐ PASS ☐ FAIL |
| 통계 없는 인덱스 | 건 | ☐ PASS ☐ FAIL |
| 히스토그램 보유 컬럼 일치 | | ☐ PASS ☐ WARN |
| 7일 초과 경과 통계 테이블 | 건 | ☐ PASS ☐ WARN |

> **STEP 05 완료 서명**: __________________ 일시: __________________

---

## STEP 06. 주의사항 체크 (05_Migration_Caution — 26항목)

**목적**: SE 제약, OCI GG 특성, Cut-over 준비사항, 상시운영 주의사항 전수 점검
**담당**: 마이그레이션 리더, 소스/타겟 DBA, OCI GG 담당자
**예상 소요**: 1시간
**실행 환경**: `[양쪽]`

---

### 6-1. SE 제약 항목

| 항목 | 내용 | 확인 결과 | 판정 |
|------|------|-----------|------|
| #1 ENABLE_GOLDENGATE_REPLICATION=TRUE | 소스 RDS 설정 확인 | | ☐ PASS ☐ FAIL |
| #2 PK 없는 테이블 ALL COLUMNS Supplemental Log | 전체 적용 확인 | | ☐ PASS ☐ FAIL |
| #3 Streams Pool 메모리 설정 | ≥ 256MB 확인 | | ☐ PASS ☐ FAIL |
| #4 SE DDL Replication 미지원 유형 식별 | 미지원 목록 작성 완료 | | ☐ PASS ☐ WARN |
| #5 BITMAP Index → 일반 Index 대체 | 소스 BITMAP Index 없음 또는 대체 완료 | | ☐ PASS ☐ FAIL |
| #6 DBMS_JOB → DBMS_SCHEDULER 전환 | 전환 매핑 완료 | | ☐ PASS ☐ WARN |

### 6-2. OCI GG 항목

| 항목 | 내용 | 확인 결과 | 판정 |
|------|------|-----------|------|
| #7 RDS Redo Log 보존 48h 이상 | archivelog retention 확인 | | ☐ PASS ☐ FAIL |
| #8 Cloud 버전 DDL 제약 릴리즈 노트 확인 | 미지원 DDL 식별 완료 | | ☐ PASS ☐ WARN |
| #9 Trail 저장소 비용/보존 정책 설정 | Object Storage Policy 설정 확인 | | ☐ PASS ☐ WARN |
| #10 RDS→OCI 구간 Latency LAG 영향 측정 | 최대 LAG 측정값 기록 | | ☐ PASS ☐ WARN |
| #11 Deployment 버전 호환성 | Oracle 19c SE 지원 확인 | | ☐ PASS ☐ FAIL |
| #12 MV 수동 재생성 완료 | 타겟 MV 목록 확인 | | ☐ PASS ☐ FAIL |
| #13 DB Link 수동 재생성 완료 | 타겟 DB Link 연결 테스트 PASS | | ☐ PASS ☐ FAIL |

### 6-3. Cut-over 준비 항목

| 항목 | 내용 | 확인 결과 | 판정 |
|------|------|-----------|------|
| #14 SCN 기반 동기화 준비 | ATCSN 설정 확인 완료 | | ☐ PASS ☐ FAIL |
| #15 Sequence 재설정 절차 확인 | 스크립트 준비 완료 | | ☐ PASS ☐ FAIL |
| #16 Trigger 재활성화 스크립트 준비 | enable_triggers.sql 준비 완료 | | ☐ PASS ☐ FAIL |
| #17 FK 재활성화 스크립트 준비 | enable_fk.sql 준비 완료 | | ☐ PASS ☐ FAIL |
| #18 JOB 활성화 절차 준비 | DBMS_SCHEDULER ENABLE 스크립트 준비 | | ☐ PASS ☐ FAIL |
| #19 DB Link 전환 테스트 완료 | 타겟 환경 DB Link 정상 동작 확인 | | ☐ PASS ☐ FAIL |

### 6-4. 상시운영 항목

| 항목 | 내용 | 확인 결과 | 판정 |
|------|------|-----------|------|
| #20 LAG 알람 30초 이하 설정 | OCI Monitoring 알람 설정 확인 | | ☐ PASS ☐ WARN |
| #21 Supplemental Logging 유지 절차 | 신규 테이블 시 즉시 적용 절차 수립 | | ☐ PASS ☐ WARN |
| #22 GGADMIN 비밀번호 만료 정책 | UNLIMITED 또는 갱신 알람 절차 | | ☐ PASS ☐ WARN |
| #23 Trail File 용량 임계치 알람 | 80% 초과 시 알람 설정 | | ☐ PASS ☐ WARN |
| #24 Discard 파일 일일 점검 체계 | 일일 점검 담당자 및 절차 수립 | | ☐ PASS ☐ WARN |
| #25 NLS 변경 금지 정책 수립 | 정책 문서화 및 승인 프로세스 확인 | | ☐ PASS ☐ WARN |
| #26 신규 테이블 GG 포함 절차 | GG 운영 중 신규 테이블 처리 절차 수립 | | ☐ PASS ☐ WARN |

### 6-5. STEP 06 결과 요약

| 분류 | 항목 수 | PASS | FAIL | WARN | 비고 |
|------|--------|------|------|------|------|
| SE 제약 (#1~6) | 6 | | | | |
| OCI GG (#7~13) | 7 | | | | |
| Cut-over 준비 (#14~19) | 6 | | | | |
| 상시운영 (#20~26) | 7 | | | | |
| **합계** | **26** | | | | |

> **STEP 06 완료 서명**: __________________ 일시: __________________

---

## STEP 07. Cut-over 직전 최종 오브젝트 완전성 확인

**목적**: GG 복제 기간 중 소스에서 발생한 DDL 변경이 타겟에 모두 반영되었는지 최종 확인
**시점**: Cut-over D-Day, 소스 차단 직전
**담당**: 소스/타겟 DBA
**예상 소요**: 30분
**실행 환경**: `[양쪽]`

---

### 7-1. 최종 INVALID 객체 0건 확인

```sql
-- [타겟] 실행 — 기대값: 0건 (Cut-over 진행 필수 조건)
SELECT OWNER, OBJECT_TYPE, OBJECT_NAME, STATUS
FROM DBA_OBJECTS
WHERE STATUS = 'INVALID'
AND OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
ORDER BY OWNER, OBJECT_TYPE;
```

**결과**: ______ 건 | 판정: ☐ PASS ☐ FAIL (INVALID 0건이어야 Go 가능)

### 7-2. 최종 UNUSABLE 인덱스 0건 확인

```sql
-- [타겟] 실행 — 기대값: 0건
SELECT OWNER, INDEX_NAME, TABLE_NAME, STATUS, PARTITIONED
FROM DBA_INDEXES
WHERE STATUS NOT IN ('VALID','N/A')
AND OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS');

-- [타겟] 파티션 인덱스 UNUSABLE 확인
SELECT INDEX_OWNER, INDEX_NAME, PARTITION_NAME, STATUS
FROM DBA_IND_PARTITIONS
WHERE STATUS != 'USABLE'
AND INDEX_OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS');
```

**결과**: ______ 건 | 판정: ☐ PASS ☐ FAIL

### 7-3. 오브젝트 유형별 COUNT 최종 비교

```sql
-- [양쪽] 실행 — 소스/타겟 동일해야 함
SELECT OBJECT_TYPE, COUNT(*) AS CNT
FROM DBA_OBJECTS
WHERE OWNER NOT IN (
    'SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS','AUDSYS','CTXSYS',
    'DBSFWUSER','DVSYS','GGSYS','GSMADMIN_INTERNAL','LBACSYS','OJVMSYS','WMSYS','XDB'
)
AND OBJECT_TYPE NOT IN ('JAVA CLASS','JAVA DATA','JAVA RESOURCE')
GROUP BY OBJECT_TYPE
ORDER BY OBJECT_TYPE;
```

| 오브젝트 유형 | 소스 COUNT | 타겟 COUNT | 차이 | 판정 |
|------------|-----------|-----------|------|------|
| TABLE | | | | ☐ PASS ☐ FAIL |
| INDEX | | | | ☐ PASS ☐ FAIL |
| VIEW | | | | ☐ PASS ☐ FAIL |
| SEQUENCE | | | | ☐ PASS ☐ FAIL |
| PROCEDURE | | | | ☐ PASS ☐ FAIL |
| FUNCTION | | | | ☐ PASS ☐ FAIL |
| PACKAGE | | | | ☐ PASS ☐ FAIL |
| PACKAGE BODY | | | | ☐ PASS ☐ FAIL |
| TRIGGER | | | | ☐ PASS ☐ FAIL |
| SYNONYM | | | | ☐ PASS ☐ FAIL |
| TYPE | | | | ☐ PASS ☐ FAIL |

### 7-4. 통계 없는 테이블/인덱스 최종 확인

```sql
-- [타겟] 기대값: 0
SELECT COUNT(*) AS NO_STATS_TABLE FROM DBA_TABLES
WHERE OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
AND LAST_ANALYZED IS NULL;

SELECT COUNT(*) AS NO_STATS_INDEX FROM DBA_INDEXES
WHERE OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
AND LAST_ANALYZED IS NULL;
```

### 7-5. Sequence GAP 최종 확인

```sql
-- [타겟] 타겟 LAST_NUMBER > 소스 현재값 확인
SELECT SEQUENCE_OWNER, SEQUENCE_NAME, LAST_NUMBER, CACHE_SIZE
FROM DBA_SEQUENCES
WHERE SEQUENCE_OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
ORDER BY SEQUENCE_OWNER, SEQUENCE_NAME;
```

### 7-6. STEP 07 결과 기록

| 항목 | 기대값 | 확인 결과 | 판정 |
|------|--------|-----------|------|
| INVALID 객체 | 0건 | | ☐ PASS ☐ FAIL |
| UNUSABLE 인덱스 | 0건 | | ☐ PASS ☐ FAIL |
| UNUSABLE 파티션 인덱스 | 0건 | | ☐ PASS ☐ FAIL |
| 오브젝트 COUNT 소스/타겟 일치 | 0 차이 | | ☐ PASS ☐ FAIL |
| 통계 없는 테이블 | 0건 | | ☐ PASS ☐ FAIL |
| 통계 없는 인덱스 | 0건 | | ☐ PASS ☐ FAIL |
| Sequence GAP 정상 | 타겟 > 소스 | | ☐ PASS ☐ WARN |

> **STEP 07 완료 서명**: __________________ 일시: __________________

---

## STEP 08. Go/No-Go 판정

**목적**: 전체 136개 검증 항목 결과를 집계하여 Cut-over 진행 여부를 최종 결정
**담당**: 마이그레이션 리더 (최종 판정), 비즈니스 담당 (업무 영향 판단)
**예상 소요**: 1시간

---

### 8-1. 전체 검증 결과 집계

| 시트 | 분류 | 총 항목 | PASS | FAIL | WARN |
|------|------|--------|------|------|------|
| 01_GG_Process | GG 프로세스 검증 | 28 | | | |
| 02_Static_Schema | 정적 구조 검증 | 38 | | | |
| 03_Data_Validation | 데이터 정합성 | 25 | | | |
| 04_Special_Objects | 특수 객체 | 42 | | | |
| 05_Migration_Caution | 주의사항 | 26 | | | |
| 통계정보 + 최종 확인 | 추가 확인 | 별도 | | | |
| **합계** | | **136+** | | | |

### 8-2. Go/No-Go 판정 기준

| 판정 | 조건 |
|------|------|
| **Go** | 중요도 [상] 항목 전체 PASS, WARN 항목 원인 분석 완료 및 비즈니스 영향 없음 확인 |
| **Conditional Go** | WARN 항목 존재하나 조치 계획 수립 완료, 책임자 서명 |
| **No-Go** | FAIL 항목 1건 이상 존재 |

**중요도 [상] 항목 (FAIL 시 무조건 No-Go)**:
- ENABLE_GOLDENGATE_REPLICATION = TRUE
- Extract/Replicat 상태 RUNNING
- Extract/Replicat LAG < 30초
- 타겟 INVALID 객체 0건
- 타겟 UNUSABLE 인덱스 0건
- 오브젝트 COUNT 소스/타겟 일치
- 핵심 테이블 Row Count 일치 (1% 이상 차이 없음)
- 핵심 테이블 데이터 정합성 PASS

### 8-3. 최종 판정 서명

| 구분 | 내용 | 서명 | 일시 |
|------|------|------|------|
| **판정 결과** | ☐ Go  ☐ Conditional Go  ☐ No-Go | | |
| FAIL 항목 수 | 건 | | |
| WARN 항목 수 | 건 | | |
| WARN 원인 분석 완료 여부 | ☐ 완료  ☐ 미완료 | | |

| 역할 | 성명 | 서명 | 일시 |
|------|------|------|------|
| 마이그레이션 리더 | | | |
| 소스 DBA | | | |
| 타겟 DBA | | | |
| OCI GG 담당자 | | | |
| 비즈니스 담당 | | | |

> **Go 판정 시 → STEP 09 Cut-over 진행**
> **No-Go 판정 시 → 이슈 해결 후 재검증**

---

# Phase 7: Cut-over

---

## STEP 09. Cut-over 사전 체크리스트

**목적**: Cut-over 실행 직전 최종 조건 확인 및 팀 준비 상태 확인
**담당**: 마이그레이션 리더
**예상 소요**: 30분
**실행 환경**: `[GG]`, `[소스]`, `[타겟]`

---

### 9-1. GG 실시간 LAG 최종 확인

```
-- [GG] GGSCI에서 실행 (Cut-over 직전 — LAG < 5초 목표)
LAG EXTRACT EXT1
LAG REPLICAT REP1
STATS REPLICAT REP1 TOTAL
```

| 항목 | 목표값 | 확인 결과 | 판정 |
|------|-------|-----------|------|
| EXT1 LAG | < 5초 | | ☐ PASS ☐ FAIL |
| REP1 LAG | < 5초 | | ☐ PASS ☐ FAIL |
| Discard 레코드 | 0건 | | ☐ PASS ☐ FAIL |

### 9-2. Cut-over 준비 스크립트 확인

| 스크립트 | 준비 상태 | 확인자 |
|---------|---------|--------|
| DBMS_JOB BROKEN 처리 스크립트 | ☐ 준비완료 | |
| Trigger ENABLE 스크립트 (enable_triggers.sql) | ☐ 준비완료 | |
| FK ENABLE 스크립트 (enable_fk.sql) | ☐ 준비완료 | |
| Sequence 재설정 스크립트 | ☐ 준비완료 | |
| DBMS_SCHEDULER ENABLE 스크립트 | ☐ 준비완료 | |
| DNS/연결 문자열 전환 절차 | ☐ 준비완료 | |
| 롤백 절차 숙지 여부 | ☐ 완료 | |

### 9-3. 팀 준비 상태 확인

| 팀 | 담당자 | Cut-over 알림 수신 | 대기 완료 |
|-----|--------|-----------------|---------|
| DBA팀 (소스) | | ☐ | ☐ |
| DBA팀 (타겟) | | ☐ | ☐ |
| OCI GG 담당자 | | ☐ | ☐ |
| 인프라팀 | | ☐ | ☐ |
| 애플리케이션팀 | | ☐ | ☐ |
| 비즈니스 담당 | | ☐ | ☐ |

> **유지보수 공지 완료**: ☐ (서비스 중단 예정 시간 30분 공지)

> **STEP 09 완료 서명**: __________________ 일시: __________________

---

## STEP 10. Cut-over 실행

**목적**: 소스를 차단하고 타겟으로 완전 전환
**담당**: 마이그레이션 리더, 소스/타겟 DBA, OCI GG 담당자
**예상 소요**: 30분 이내 (목표)
**실행 환경**: `[소스]`, `[타겟]`, `[GG]`

> **Cut-over 시작 시각 기록**: ______________________

---

### 10-1. Step 1: 소스 DB 애플리케이션 세션 차단

```bash
# 앱 서버 다운 또는 소스 RDS 인바운드 1521 포트 차단
# [AWS콘솔] RDS 보안그룹 → 인바운드 규칙 → TCP 1521 삭제 또는 비활성화
```

```sql
-- [소스] 잔여 세션 확인 (0건 목표)
SELECT COUNT(*) AS USER_SESSION_CNT
FROM V$SESSION
WHERE TYPE = 'USER' AND USERNAME != 'GGADMIN';
-- 기대값: 0건 (GGADMIN 제외)
```

### 10-2. Step 2: 소스 DBMS_JOB BROKEN 처리

```sql
-- [소스] 실행
SPOOL /tmp/broken_jobs.sql
SELECT 'EXEC DBMS_JOB.BROKEN(' || JOB || ', TRUE);'
FROM DBA_JOBS WHERE BROKEN = 'N';
SPOOL OFF
@/tmp/broken_jobs.sql
COMMIT;
-- 확인
SELECT JOB, BROKEN FROM DBA_JOBS;
-- 기대값: 모든 JOB BROKEN = 'Y'
```

### 10-3. Step 3: 소스 CURRENT_SCN 최종 기록

```sql
-- [소스] 실행
SELECT CURRENT_SCN, TO_CHAR(SYSDATE,'YYYY-MM-DD HH24:MI:SS') AS CUT_TIME
FROM V$DATABASE;
```

**최종 SCN 기록**: __________________ | **Cut-over 시각**: __________________

### 10-4. Step 4: GG LAG = 0 확인 대기 (타임아웃: 30분)

```
-- [GG] GGSCI에서 반복 실행 (30초 간격)
LAG EXTRACT EXT1     -- 목표: 0초
LAG REPLICAT REP1    -- 목표: 0초
STATS REPLICAT REP1 TOTAL  -- 더 이상 카운트 증가 없음 확인
```

| 확인 시각 | EXT1 LAG | REP1 LAG | 판정 |
|---------|---------|---------|------|
| | | | |
| | | | |
| | 0초 | 0초 | ☐ PASS |

> **30분 초과 시**: 롤백 절차 검토

### 10-5. Step 5~6: Replicat 중지 및 HANDLECOLLISIONS 제거

```
-- [GG] GGSCI에서 실행
STOP REPLICAT REP1

EDIT PARAMS REP1
-- HANDLECOLLISIONS 라인 이미 제거되었는지 확인 (02_migration.md STEP 18-4에서 제거 완료 여부 확인)
-- 남아있다면 이 시점에 제거
```

### 10-6. Step 7: 타겟 DB 최종 데이터 검증

```sql
-- [타겟] 핵심 테이블 최신 데이터 확인
SELECT MAX(<UPDATED_DATE_COL>) AS LATEST_DATA FROM <SCHEMA>.<KEY_TABLE>;
-- 소스 차단 직전 시각과 동일하거나 가까운 값 확인

-- [양쪽] 핵심 테이블 Row Count 최종 비교
SELECT COUNT(*) FROM <SCHEMA>.<KEY_TABLE>;
```

### 10-7. Step 8: Cut-over 완료 처리

#### 8a. Trigger 재활성화

```sql
-- [타겟] Trigger ENABLE 스크립트 실행
SPOOL /tmp/enable_triggers.sql
SELECT 'ALTER TRIGGER ' || OWNER || '.' || TRIGGER_NAME || ' ENABLE;'
FROM DBA_TRIGGERS
WHERE OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
ORDER BY OWNER;
SPOOL OFF
@/tmp/enable_triggers.sql
-- 확인: ENABLED Trigger 건수
SELECT COUNT(*) FROM DBA_TRIGGERS
WHERE STATUS = 'ENABLED'
AND OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS');
```

#### 8b. FK Constraint 재활성화

```sql
-- [타겟] FK ENABLE 스크립트 실행
SPOOL /tmp/enable_fk.sql
SELECT 'ALTER TABLE ' || OWNER || '.' || TABLE_NAME ||
       ' ENABLE CONSTRAINT ' || CONSTRAINT_NAME || ';'
FROM DBA_CONSTRAINTS
WHERE CONSTRAINT_TYPE = 'R' AND STATUS = 'DISABLED'
AND OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
ORDER BY OWNER, TABLE_NAME;
SPOOL OFF
@/tmp/enable_fk.sql
-- 오류 발생 시: Orphan Row 제거 후 재시도
-- 확인: DISABLED FK 0건
SELECT COUNT(*) FROM DBA_CONSTRAINTS
WHERE CONSTRAINT_TYPE = 'R' AND STATUS = 'DISABLED'
AND OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS');
```

#### 8c. Sequence 최종값 재설정

```
-- [GG] GGSCI에서 GG 프로세스 완전 중지
STOP EXTRACT EXT1
STOP EXTRACT PUMP1
```

```sql
-- [소스] 각 시퀀스 현재 LAST_NUMBER 조회
SELECT SEQUENCE_OWNER, SEQUENCE_NAME, LAST_NUMBER, CACHE_SIZE,
       LAST_NUMBER + (CACHE_SIZE * 10) AS SAFE_VALUE
FROM DBA_SEQUENCES
WHERE SEQUENCE_OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
ORDER BY SEQUENCE_OWNER, SEQUENCE_NAME;
```

```sql
-- [타겟] Sequence 재설정 (소스 LAST_NUMBER + CACHE_SIZE * 10 이상으로)
ALTER SEQUENCE <SCHEMA>.<SEQ_NAME> RESTART START WITH <SAFE_VALUE>;
-- ※ RESTART START WITH 미지원 Oracle 버전:
-- ALTER SEQUENCE <SEQ_NAME> INCREMENT BY <DIFF>;
-- SELECT <SEQ_NAME>.NEXTVAL FROM DUAL;
-- ALTER SEQUENCE <SEQ_NAME> INCREMENT BY 1;
```

#### 8d. DBMS_SCHEDULER JOB 활성화

```sql
-- [타겟] 실행
BEGIN
    DBMS_SCHEDULER.ENABLE(name => '<SCHEMA>.<JOB_NAME>');
END;
/
-- 전체 JOB ENABLE 후 상태 확인
SELECT JOB_NAME, STATE, NEXT_RUN_DATE FROM DBA_SCHEDULER_JOBS
WHERE OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
ORDER BY OWNER, JOB_NAME;
```

#### 8e. DB Link 연결 테스트

```sql
-- [타겟] 실행
SELECT * FROM DUAL@<DB_LINK_NAME>;
-- 기대값: DUMMY = 'X'
```

### 10-8. Step 9~10: 애플리케이션 연결 전환 및 정상 동작 확인

```
[인프라팀] DNS 변경 또는 애플리케이션 연결 문자열 타겟 OCI DBCS로 전환
[애플리케이션팀] 앱 서버 기동
[애플리케이션팀] 핵심 기능 Smoke Test 실행 (30분)
[모니터링] 에러 로그 모니터링 개시
```

> **Cut-over 완료 시각 기록**: ______________________
> **총 소요 시간**: ______ 분

---

## STEP 11. Cut-over 후 즉시 검증

**목적**: Cut-over 직후 타겟 DB 상태 이상 여부 즉시 점검
**담당**: 타겟 DBA, 애플리케이션 담당
**예상 소요**: 30분
**실행 환경**: `[타겟]`

---

### 11-1. 데이터 최신성 확인

```sql
-- [타겟] 소스 차단 직전 데이터가 타겟에 있는지 확인
SELECT MAX(<UPDATED_DATE_COL>) AS LATEST_DATA
FROM <SCHEMA>.<KEY_TABLE>;
-- 기대값: 소스 차단 시각과 동일하거나 직전
```

### 11-2. 오브젝트 상태 최종 확인

```sql
-- [타겟] Trigger 활성화 확인 (기대값: 0건 DISABLED)
SELECT COUNT(*) AS DISABLED_TRIGGER_CNT FROM DBA_TRIGGERS
WHERE STATUS = 'DISABLED'
AND OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS');

-- [타겟] FK 활성화 확인 (기대값: 0건 DISABLED)
SELECT COUNT(*) AS DISABLED_FK_CNT FROM DBA_CONSTRAINTS
WHERE STATUS = 'DISABLED' AND CONSTRAINT_TYPE = 'R'
AND OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS');

-- [타겟] Sequence 현재값 확인 (소스 최댓값보다 큰지)
SELECT SEQUENCE_OWNER, SEQUENCE_NAME, LAST_NUMBER
FROM DBA_SEQUENCES
WHERE SEQUENCE_OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
ORDER BY SEQUENCE_OWNER, SEQUENCE_NAME;

-- [타겟] SCHEDULER JOB 활성화 확인
SELECT JOB_NAME, STATE FROM DBA_SCHEDULER_JOBS
WHERE OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
AND STATE = 'DISABLED';
-- 기대값: 0건

-- [타겟] INVALID 객체 최종 확인 (기대값: 0건)
SELECT COUNT(*) AS INVALID_CNT FROM DBA_OBJECTS
WHERE STATUS = 'INVALID'
AND OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS');

-- [타겟] UNUSABLE 인덱스 최종 확인 (기대값: 0건)
SELECT COUNT(*) AS UNUSABLE_IDX_CNT FROM DBA_INDEXES
WHERE STATUS NOT IN ('VALID','N/A')
AND OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS');

-- [타겟] 통계 없는 테이블 최종 확인 (기대값: 0건)
SELECT COUNT(*) AS NO_STATS_CNT FROM DBA_TABLES
WHERE OWNER NOT IN ('SYS','SYSTEM','OUTLN','DBSNMP','APPQOSSYS')
AND LAST_ANALYZED IS NULL;
```

### 11-3. Cut-over 후 즉시 검증 결과

| 항목 | 기대값 | 확인 결과 | 판정 |
|------|--------|-----------|------|
| 최신 데이터 확인 | 소스 차단 시각 기준 | | ☐ PASS ☐ FAIL |
| DISABLED Trigger | 0건 | | ☐ PASS ☐ FAIL |
| DISABLED FK | 0건 | | ☐ PASS ☐ FAIL |
| Sequence 현재값 (소스 최댓값 이상) | 정상 | | ☐ PASS ☐ FAIL |
| DISABLED SCHEDULER JOB | 0건 | | ☐ PASS ☐ WARN |
| INVALID 객체 | 0건 | | ☐ PASS ☐ FAIL |
| UNUSABLE 인덱스 | 0건 | | ☐ PASS ☐ FAIL |
| 통계 없는 테이블 | 0건 | | ☐ PASS ☐ WARN |
| Smoke Test 결과 | PASS | | ☐ PASS ☐ FAIL |
| 에러 로그 (30분간 모니터링) | 오류 없음 | | ☐ PASS ☐ WARN |

> ❌ **FAIL 항목 발생 시**: 마이그레이션 리더에게 즉시 보고 → 30분 이내 롤백 여부 결정

**Cut-over 성공 최종 확인**

| 역할 | 성명 | 서명 | 일시 |
|------|------|------|------|
| 마이그레이션 리더 | | | |
| 타겟 DBA | | | |
| 애플리케이션 담당 | | | |
| 비즈니스 담당 | | | |

---

# Phase 8: Cut-over 후 안정화

---

## STEP 12. Cut-over 후 안정화 모니터링

**목적**: Cut-over 후 D+14일까지 시스템 안정성 모니터링 및 소스 RDS 유지/종료 결정
**담당**: 타겟 DBA, OCI GG 담당자
**기간**: D+1 ~ D+14

---

### 12-1. 상시 모니터링 Top 10 체크리스트

| 순위 | 항목 | 조치 기준 | 담당자 |
|------|------|-----------|--------|
| 1 | Extract/Replicat LAG 상시 모니터링 | LAG > 30초 즉시 알람 → 원인 분석 | |
| 2 | PK 없는 테이블 ALL COLUMNS Logging 유지 | 신규 테이블 생성 시 즉시 적용 | |
| 3 | Sequence GAP 누적 관리 | 주간 Gap 확인, 필요 시 재설정 | |
| 4 | Trigger 이중 실행 방지 | GGADMIN 세션 Trigger 예외 로직 확인 | |
| 5 | DDL Replication ABEND 관리 | 미지원 DDL 수동 처리 즉시 적용 | |
| 6 | RDS Redo Log 보존 기간 관리 | 최소 24h 이상 유지 | |
| 7 | GGADMIN 비밀번호 만료 모니터링 | 만료 30일 전 알람 → 즉시 갱신 | |
| 8 | LOB 컬럼 Trail File 용량 급증 | Object Storage 80% 초과 시 알람 | |
| 9 | Constraint DISABLED 상태 정기 점검 | 주간 점검 — Disabled 발견 시 원인 분석 | |
| 10 | NLS 파라미터/타임존 변경 금지 | 변경 금지 정책 — 승인 프로세스 필수 | |

### 12-2. Discard 파일 일일 점검

```
-- [GG] GGSCI — 매일 1회 이상 실행
VIEW DISCARD REP1
-- Discard 레코드 > 0건 시: 원인 분석 → 재적용 또는 데이터 수정 후 확인
```

### 12-3. 일일 모니터링 기록표

| 날짜 | EXT1 LAG | REP1 LAG | Discard 건수 | ABEND | 특이사항 | 확인자 |
|------|---------|---------|------------|-------|---------|--------|
| D+1 | | | | | | |
| D+2 | | | | | | |
| D+3 | | | | | | |
| D+7 | | | | | | |
| D+14 | | | | | | |

### 12-4. 소스 RDS 유지 및 종료 계획

| 일정 | 작업 | 담당자 | 완료 서명 |
|------|------|--------|---------|
| D+1 ~ D+14 | 소스 RDS 유지 (롤백 대비) | | |
| D+7 | 타겟 안정화 확인 후 소스 → Read-Only 전환 검토 | | |
| D+14 | 이상 없음 확인 시 소스 RDS 종료 및 비용 절감 | | |
| 최종 | OCI GG Deployment 종료 또는 상시 동기화 모드 결정 | | |

### 12-5. 롤백 결정 트리거 조건

| 조건 | 심각도 | 결정 타임아웃 | 담당자 |
|------|--------|-------------|--------|
| 애플리케이션 심각한 기능 오류 | 즉시 | 30분 이내 | 마이그레이션 리더 |
| 데이터 무결성 문제 발견 | 즉시 | 30분 이내 | 마이그레이션 리더 |
| 타겟 DB 성능 소스 대비 30% 이상 저하 | 1시간 이내 | 1시간 | 마이그레이션 리더 |
| Cut-over 후 4시간 경과, 이슈 미해결 | 자동 결정 | — | 마이그레이션 리더 |

> **롤백 절차**: `plan/migration_plan.md > 롤백 계획` 섹션 참조

---

## 이슈 로그

| # | 발생 일시 | Phase/STEP | 이슈 내용 | 원인 분석 | 조치 내용 | 해결 일시 | 담당자 |
|---|---------|------------|-----------|-----------|-----------|---------|--------|
| 1 | | | | | | | |
| 2 | | | | | | | |
| 3 | | | | | | | |
| 4 | | | | | | | |
| 5 | | | | | | | |

---

## 긴급 연락처

| 상황 | 연락 대상 | 연락처 |
|------|-----------|--------|
| GG ABEND / FAIL 항목 발생 | OCI GG 담당자 | |
| 롤백 결정 | 마이그레이션 리더 | |
| 애플리케이션 오류 | 애플리케이션 담당 | |
| OCI 인프라 이슈 | OCI 인프라 담당 | |
| AWS RDS 이슈 | 소스 DBA | |
